import { Routes } from "@angular/router";
import { LoginComponent } from "./features/login/login/login.component";

export const routes: Routes = [{ component: LoginComponent, path: "login",title: 'login' }];
